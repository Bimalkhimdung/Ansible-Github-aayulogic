name: Running Scripts
on:
   push:
     branches:
        - Deploy
 #workflow_dispatch:
jobs:
  ssh-to-remote:
    runs-on: ubuntu-latest

    steps:
     - name: checkout code
       uses: actions/checkout@v2
     - name: Running Script in  Server
       uses: appleboy/ssh-action@master
       with:
         host: 192.168.102.238
         username: devops
         key: ${{ secrets.SERVER_PRIVATE_KEY_MAIN }}
      
         script: |
              pwd 
              ls
              
